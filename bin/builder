#!/usr/bin/env node --harmony

/**
 * Module Dependencies
 */

var build = require('./../');
var bundled = '--bundled' === process.argv[2];
var co = require('co');
var params = {
  copy: true,
  bundled: bundled
};

/**
 * Build
 */

co(build(params))();
